# AGENTS.md

本文件说明本项目 `design.md` 的内容格式与维护规则；项目内持续按此格式维护 `design.md`。

## design.md 内容格式

`design.md` 以**业务语言**描述当前项目的模块、功能及内容，结构为：

1. **项目定位**：一句话说明项目是什么、解决什么问题；
2. **总体结构**：crate 布局、二进制产物、关键目录；
3. **功能模块**：按模块分节，每节说明职责、启用方式（feature/环境变量）
   与平台支持；
4. **构建与发布**：feature 体系、常用命令、CI 策略；
5. **与其他系统的关系**：外部契约与协作方（如有）。

## 来源标记规则

design.md 中**每一项**内容都必须分别标记来源：

- **【用户要求】**：用户明确提出的改动、约束或需求（含原始诉求的语义）；
- **【AI 演绎】**：AI 为落实用户要求而自行推导、补全的细节（命名、
  拆分方式、CI 矩阵等），尚未经用户逐项确认。

## 维护规则

1. 结构性改动（模块增删、feature 变化、二进制/环境变量/契约变更）落地时，
   同步更新 design.md 对应条目并标注来源与日期；
2. 用户指出 design.md 某些内容不对时：修正错误、收敛含糊表述，并把被
   确认的【AI 演绎】条目改标为【用户要求】（含确认语义），使未经确认的
   演绎内容越来越少，避免同一问题反复修改；
3. 后续任何演绎（补全或扩展设计）以用户要求为边界，只在用户未确认的
   地方做调整，不得擅自改动用户已明确的需求。
